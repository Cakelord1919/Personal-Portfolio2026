import { renderCaseStudy } from './case-study.mjs';
import { renderBluCaseStudy } from './blu-case-study.mjs';
import { renderPersonalGallery, renderPersonalDetail } from './personal-work.mjs';
import fs from 'node:fs';
import path from 'node:path';
const site=JSON.parse(fs.readFileSync('content/site.json','utf8'));
const personalWork=JSON.parse(fs.readFileSync('content/personal-work.json','utf8'));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
if(!/^https:\/\//.test(site.siteUrl)||!/^https:\/\//.test(site.contactUrl))throw Error('Use HTTPS URLs');
const slugs=new Set();
for(const p of site.projects){if(!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(p.slug)||slugs.has(p.slug))throw Error('Invalid or duplicate slug');slugs.add(p.slug);if(p.image&&(!/^assets\/[a-zA-Z0-9/_.-]+$/.test(p.image)||p.image.includes('..')||!fs.existsSync('public/'+p.image)))throw Error('Missing/invalid project image');}
for(const p of personalWork){if(!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(p.slug)||slugs.has(p.slug))throw Error('Invalid or duplicate personal work slug');slugs.add(p.slug);for(const image of [p.cover,...p.images]){if(!/^assets\/[a-zA-Z0-9/_.-]+$/.test(image)||image.includes('..')||!fs.existsSync('public/'+image))throw Error(`Missing/invalid personal work image: ${image}`);}}
fs.rmSync('dist',{recursive:true,force:true});
fs.mkdirSync('dist',{recursive:true});
for(const f of ['styles.css','main.js'])fs.copyFileSync('src/'+f,'dist/'+f);
fs.cpSync('public','dist',{recursive:true});
const base=site.siteUrl.replace(/\/?$/,'/');
const head=(title,description,url,prefix='')=>`<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${esc(url)}"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:type" content="website"><meta property="og:url" content="${esc(url)}"><meta name="theme-color" content="#ffe500"><script>try{if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&sessionStorage.getItem('page-transition')==='1'){document.documentElement.classList.add('page-entering');sessionStorage.removeItem('page-transition')}}catch{}</script><link rel="icon" href="${prefix}favicon.svg?v=2" type="image/svg+xml"><link rel="stylesheet" href="${prefix}styles.css"><script src="${prefix}main.js" defer></script></head><body><div class="page-transition" aria-hidden="true"><div class="transition-mark"><img class="transition-arrow" src="${prefix}assets/transition-arrow.svg" width="116" height="52" alt=""><span class="transition-signature">CAKELORD /<br>ERIC GABRIEL CHENG LI</span></div></div><a class="skip" href="#main">Skip to content</a>`;
const art=(p,prefix='')=>p.image?`<img src="${prefix}${esc(p.image)}" alt="${esc(p.imageAlt||p.title)}" loading="lazy" width="1200" height="900">`:`<div class="art art-${esc(p.theme)}" aria-hidden="true">${p.theme==='form'?'<div class="orbit"></div><span>FORM<br>STUDY.</span>':p.theme==='type'?'<span>Aa<br>Bb.</span><i>THE SHAPE OF WORDS / 02</i>':'<div class="rings"></div><span>IN<br>MOTION.</span>'}</div>`;
const footer=(prefix='')=>`<footer><a class="wordmark" href="${prefix||'./'}">${esc(site.name)}<span>®</span></a><span>© ${new Date().getFullYear()} / PERSONAL PORTFOLIO</span><a href="#top">Back to top ↑</a></footer><div class="cursor" aria-hidden="true"></div></body></html>`;
const cards=site.projects.map((p,i)=>`<article class="project reveal${p.underConstruction?' project-construction':''}" data-category="${esc(p.category)}"><a class="project-link" href="projects/${p.slug}/"><div class="cover">${art(p)}${p.underConstruction?'<span class="construction-tape tape-a">UNDER CONSTRUCTION / UNDER CONSTRUCTION /</span><span class="construction-tape tape-b">UNDER CONSTRUCTION / UNDER CONSTRUCTION /</span>':''}<span class="view">${p.underConstruction?'Under Construction X':'View project ↗'}</span></div><div class="project-meta"><span>0${i+1} / ${esc(p.category)}</span><span>${esc(p.year)}${p.demo?' · Layout demo':''}</span></div><h3>${esc(p.title)}</h3><p>${esc(p.summary)}</p></a></article>`).join('');
const categories=[...new Set(site.projects.map(p=>p.category))];
const personalGallery=renderPersonalGallery(personalWork,esc);
fs.writeFileSync('dist/index.html',head(site.title,site.description,base)+`<header id="top"><a class="wordmark" href="#top">${esc(site.name)}<span>®</span></a><nav aria-label="Main navigation"><a href="#work">WORK</a><a href="#personal-work">PERSONAL</a><a href="#about">ABOUT</a><a href="#contact">Contact ↗</a></nav><span class="edition">PORTFOLIO — 2026</span></header><main id="main"><section class="hero" aria-labelledby="hero-title"><div class="micro hero-label">ERIC GABRIEL CHENG LI<br>-<br>GAME DESIGNER<br>GRAPHIC DESIGNER<br>&amp; A 3D ARTIST</div><div class="hero-gradient" aria-hidden="true"></div><span class="micro hero-side">#001<br>ALWAYS EXPLORING</span><div class="hero-bottom"><h1 id="hero-title" class="animated-title" aria-label="See more. Scroll down."><span class="title-line" data-title-line aria-hidden="true">See</span><span class="title-line outline" data-title-line aria-hidden="true">More</span></h1><div class="hero-note"><p>${esc(site.intro)}</p><a class="round-link" href="#work" aria-label="Explore selected work">↓</a></div></div><div class="hero-foot micro"><span>INTERACTION/ MODELLING/ ART/ STUDENT WORK</span><span>SCROLL TO EXPLORE ↓</span></div></section><section id="work" class="work"><div class="section-head"><span class="micro">01 / SELECTED WORK</span><h2>Ideas<br>take shape<span class="yellow-dot">.</span></h2><p>Selected projects and explorations.</p></div><div class="filters" role="group" aria-label="Filter projects"><button class="active" data-filter="All" aria-pressed="true">All <sup>${site.projects.length.toString().padStart(2,'0')}</sup></button>${categories.map(c=>`<button data-filter="${esc(c)}" aria-pressed="false">${esc(c)}</button>`).join('')}</div><div class="projects">${cards}</div><p id="empty" hidden>No projects in this category yet.</p></section><section id="personal-work" class="personal-work"><div class="personal-heading"><span class="micro">02 / PERSONAL WORK GALLERY</span><h2>Outside<br>the brief<span class="yellow-dot">.</span></h2><div><p>Self-directed 3D, environment, concept, and graphic experiments.</p><a href="https://www.artstation.com/eric-gabriel-cheng-li" target="_blank" rel="noopener noreferrer">All work on ArtStation ↗</a></div></div><div class="personal-masonry">${personalGallery}</div></section><section id="about" class="about"><span class="micro">03 / A LITTLE ABOUT ME</span><div><h2>Stay curious.<br>Keep exploring<span class="yellow-dot">.</span></h2><p>${esc(site.about)}</p><div class="about-tags"><span>VISUAL</span><span>INTERACTION</span><span>EXPERIMENT</span></div></div></section><section id="contact" class="contact"><span class="micro">04 / KEEP IN TOUCH</span><button class="contact-trigger" aria-expanded="false" aria-controls="contact-links"><span>LET’S<br>TALK.</span><span class="contact-arrow" aria-hidden="true">↗</span><span class="sr-only">Show contact options</span></button><p>FIND ME / GET IN TOUCH</p><div id="contact-links" class="contact-links" inert aria-label="Contact options">${(site.socialLinks||[]).map((link,i)=>{const valid=/^https:\/\//.test(link.url)||/^mailto:[^\s@]+@[^\s@]+$/.test(link.url);return `<${valid?'a':'div'} class="contact-strip" style="--order:${i}" ${valid?`href="${esc(link.url)}" ${link.url.startsWith('https:')?'target="_blank" rel="noopener noreferrer"':''}`:'aria-disabled="true"'}><span class="strip-number">0${i+1}</span><span class="strip-name">${esc(link.label)}</span><span class="strip-end">${valid?'↗':'Coming soon'}</span></${valid?'a':'div'}>`;}).join('')}</div></section></main><nav class="dock" aria-label="Quick navigation"><a href="#work">Work</a><span>/</span><a href="#personal-work">Personal</a><span>/</span><a href="#about">About</a><span>/</span><a href="#contact">Contact ↗</a></nav>`+footer());
for(const [i,p]of site.projects.entries()){const dir='dist/projects/'+p.slug;fs.mkdirSync(dir,{recursive:true});const next=site.projects.length>1?`<a class="next" href="../${site.projects[(i+1)%site.projects.length].slug}/">Next project <span>↗</span></a>`:'';const caseContent=p.slug==='blu'?renderBluCaseStudy(p,esc):renderCaseStudy(p,esc);fs.writeFileSync(dir+'/index.html',head(p.title+' — '+site.name,p.summary,base+'projects/'+p.slug+'/', '../../')+`<header id="top"><a class="wordmark" href="../../">${esc(site.name)}<span>®</span></a><nav aria-label="Project navigation"><a href="../../#work">← Back to work</a></nav><span class="edition">PROJECT / 0${i+1}</span></header><main id="main" class="detail ${p.caseStudy?'case-study':''} ${p.slug==='blu'?'blu-case':''}">${p.caseStudy?caseContent:`<div class="detail-head"><span class="micro">${esc(p.category)} / ${esc(p.year)}${p.demo?' / Layout demo':''}</span><h1>${esc(p.title)}</h1><p>${esc(p.summary)}</p></div><div class="detail-art">${art(p,'../../')}</div><section class="detail-body"><span class="micro">ABOUT THE PROJECT</span><p>${esc(p.description)}</p></section>`}${next}</main>`+footer('../../'));}
for(const [i,p]of personalWork.entries()){const dir='dist/personal/'+p.slug;fs.mkdirSync(dir,{recursive:true});fs.writeFileSync(dir+'/index.html',head(p.title+' — '+site.name,p.summary,base+'personal/'+p.slug+'/', '../../')+`<header id="top"><a class="wordmark" href="../../">${esc(site.name)}<span>®</span></a><nav aria-label="Personal work navigation"><a href="../../#personal-work">← Back to gallery</a></nav><span class="edition">PERSONAL / ${String(i+1).padStart(2,'0')}</span></header><main id="main" class="personal-detail">${renderPersonalDetail(p,personalWork,esc)}</main>`+footer('../../'));}
fs.writeFileSync('dist/sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[base,...site.projects.map(p=>base+'projects/'+p.slug+'/'),...personalWork.map(p=>base+'personal/'+p.slug+'/')].map(u=>`<url><loc>${esc(u)}</loc></url>`).join('')}</urlset>`);
fs.writeFileSync('dist/robots.txt',`User-agent: *\nAllow: /\nSitemap: ${base}sitemap.xml\n`);
fs.writeFileSync('dist/404.html',head('Page not found — '+site.name,'This page does not exist',base)+`<main class="detail"><h1>404 / LOST IN SPACE.</h1><p>This page does not exist or may have moved.</p><a href="${esc(base)}">Back to home ↗</a></main></body></html>`);
fs.writeFileSync('dist/.nojekyll','');
console.log(`Built homepage + ${site.projects.length} case studies + ${personalWork.length} personal work pages in dist/`);







